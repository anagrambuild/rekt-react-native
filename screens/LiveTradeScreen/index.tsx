import { Text } from 'react-native';

import { Column, ScreenContainer } from '@/components';

export const LiveTradeScreen = () => {
  return (
    <ScreenContainer>
      <Column>
        <Text>Live Trade</Text>
      </Column>
    </ScreenContainer>
  );
};
